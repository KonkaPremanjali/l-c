#include <stdio.h>

int main()
{
    int base,height,area;
    printf("enter b value:");
    scanf("%d",&base);
    printf("enter h value:");
    scanf("%d",&height);
    area=(base*height)/2;
    printf("area of triangle:%d",area);

    return 0;
}  //are of triangle